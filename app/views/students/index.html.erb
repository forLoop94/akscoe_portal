<p style="color: green"><%= notice %></p>

<h1>Students</h1>

<div id="students" class="grid">
  <% @students.each do |student| %>
    <%= render student %>
  <% end %>
</div>

<%= link_to "New student", new_student_path %>
